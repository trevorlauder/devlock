---
title: security-probe profile
description: Broad profile used for running devlock's own escape-probe tests. Not for normal use.
---

The `security-probe` profile is used internally to run devlock's escape-probe binaries and end-to-end tests. It opens every built-in partial and grants access to a wide set of package registries.

This profile is not intended for normal agent use.
